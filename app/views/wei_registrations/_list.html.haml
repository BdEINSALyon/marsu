.list-group
  - wei_registrations.each do |wei_registration|
    = link_to wei_registration.student.name, wei_registration, :class => ['list-group-item',('bg-success' if wei_registration.paid and wei_registration.status == 'registered'), ('bg-warning' if wei_registration.status == 'waiting' and wei_registration.paid), ('btn-outline-danger' unless wei_registration.paid)]