!!!
%html
  %head
    - if content_for? :title
      %title
        = yield(:title)
        =' - MARSU'
    - else
      %title MARSU
    = render 'layouts/headers'
  %body
    = render 'layouts/navbar'
    #main.container
      = flash_messages
      = yield