$('#memberships_card').html("<%= escape_javascript(render('students/memberships_card', student: @payment.student)) %>");
$('#student_card').html("<%= escape_javascript(render('students/student_card', student: @payment.student)) %>");